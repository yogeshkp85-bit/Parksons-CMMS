// Auth routes are registered directly in src/routes/index.ts
// This file is kept as a placeholder for future route modularisation.
// When migrating, move POST /auth/login, POST /auth/register,
// GET /auth/register-metadata, POST /auth/logout here.
import { Router } from 'express';
export const router = Router();

